server {
    listen 80;
    listen [::]:80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # API proxy → canchallena-api (mismo dominio, evita CORS)
    location /api/ {
        proxy_pass http://canchallena-api:3018;  # sin trailing slash para preservar /api
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA fallback: rutas como /demo sirven index.html (no 404)
    # NO cachear el HTML raíz: force re-validation para que siempre
    # cargue el bundle con hash nuevo y no sirva una landing vieja.
    location / {
        try_files $uri /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
